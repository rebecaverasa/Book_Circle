import React from 'react';
import { Text } from 'react-native-paper';

const QueroLer: React.FC = () => {
  return <Text style={{ marginTop: 200, textAlign: 'center' }}>💡 Livros que você quer ler</Text>;
};

export default QueroLer;
